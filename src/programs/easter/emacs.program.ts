import type { Program } from '../../kernel/program';
const prog: Program = {
  name: 'emacs',
  manpage: 'emacs — same.',
  category: 'easter',
  mode: 'inline',
  onCommand: () => 'same.',
};
export default prog;
